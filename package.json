{
  "name": "js-type-check",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm lerna run build",
    "publish": "pnpm build && pnpm lerna publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Michel Wene",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.2.1"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}